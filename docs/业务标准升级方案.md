# 业务标准升级方案

本文档用于整理业务标准相关功能的升级改造方案，覆盖标准库页面、业务规则页面、存储过程拆分以及外围系统对接等内容。

当前版本已整理到改造点 12，统一按以下结构编写：

- 改造目标
- 逻辑改造
- 页面改造
- 影响范围及兼容性说明

## 改造点总览

| 序号 | 改造点 |
| --- | --- |
| 1 | 关键数据算法下拉改为本地 JSON 配置 |
| 2 | 业务内容分类改为自有维护并通过自有接口取值 |
| 3 | 标准库新增自定义编码字段 |
| 4 | 标准库新增业务办理分类字段并优化新增、修改页面展示 |
| 5 | 业务办理标准支持配置互斥关系 |
| 6 | 关键数据算法按不同子存储过程执行 |
| 7 | 第三方程序对接范围补齐 |
| 8 | 业务办理标准值在清册选择列表中按 X 占位反显 |
| 9 | 调试功能支持业务参数自动生成及成功案例保存反显 |
| 10 | 标准库新增提示语属性名称和提示语属性单位 |
| 11 | 标准库属性组支持自定义属性录入 |
| 12 | 业务规则页面新增 AI 政策分析辅助清册勾选 |

## 改造点 1：关键数据算法下拉改为本地 JSON 配置

### 1. 改造目标

将当前页面中写死的关键数据算法 1/2/3/4，改为通过本地 UTF-8 JSON 文件统一维护，并在服务启动时读取到内存后使用。

### 2. 逻辑改造

#### 2.1 配置文件

配置文件路径：server/config/business-algorithms.json

建议配置结构：

| 配置项 | 示例值 | 说明 |
| --- | --- | --- |
| value | 1 | 算法编码，对应 gjsjsf / ywsf |
| label | 最大可提取额 | 算法名称 |
| sort | 1 | 排序号 |
| enabled | true | 是否启用 |

建议初始配置保留现有 4 个算法：

| 编码 | 名称 |
| --- | --- |
| 1 | 最大可提取额 |
| 2 | 最高可贷金额 |
| 3 | 最高可贷年限 |
| 4 | 借款人最大可对冲支取金额 |

#### 2.2 后端处理

- 服务启动时统一读取 JSON 文件并缓存到内存。
- 下拉数据、名称映射和算法合法性校验统一使用该内存配置。
- 配置变更采用“修改文件后重启服务生效”的方式。

#### 2.3 存储与校验

- gjj_ywbzk.gjsjsf 和 gjj_ywbz.ywsf 继续保存算法编码。
- 不新增数据库表，不调整现有字段结构。
- 标准库和业务规则相关保存接口增加算法编码合法性校验。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库页面（server/templates/pages/business_standard.j2） | 查询区算法下拉改为动态取值；新增、编辑弹窗中的算法下拉改为动态取值；列表中的算法名称展示改为动态映射。 |
| 业务规则页面（server/templates/pages/business_rule.j2） | 查询区算法下拉改为动态取值；清册选择弹窗中的算法相关筛选改为动态取值；列表中的算法名称展示改为动态映射。 |

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 配置文件 | server/config/business-algorithms.json |
| 页面模板 | server/templates/pages/business_standard.j2；server/templates/pages/business_rule.j2 |
| 后端逻辑 | 算法配置读取、缓存、名称映射、接口校验 |
| 兼容性说明 | 不改数据库表结构；不改现有算法编码字段；不影响现有 1/2/3/4 数据；后续新增算法时只需增加 JSON 配置并同步补充对应逻辑。 |

### 5. 已上线数据库投产 SQL

本改造点仅涉及本地 JSON 配置和服务启动时的内存缓存，不涉及数据库新增表或新增字段。

- Oracle：无需执行数据库 DDL。
- 达梦：无需执行数据库 DDL。
- 人大金仓：无需执行数据库 DDL。

## 改造点 2：业务内容分类改为自有维护并通过自有接口取值

### 1. 改造目标

将当前依赖平台接口返回的业务内容分类，改为由系统自有表维护并通过自有接口提供，同时支持在标准库页面中新增、修改、删除业务内容分类。

业务内容分类与关键数据算法存在关联关系，因此分类基础数据需按关键数据算法维度维护。

### 2. 逻辑改造

#### 2.1 业务内容分类基础表

新增表：gjj_ywnrfl

建议字段：

| 字段名 | 说明 |
| --- | --- |
| id | 主键 |
| gjsjsf | 所属关键算法编码 |
| flbm | 业务内容分类编码 |
| flmc | 业务内容分类名称 |
| pxh | 排序号 |
| sfqy | 是否启用 |
| cjsj | 创建时间 |
| gxsj | 更新时间 |

#### 2.2 下拉接口

- 原 tools/business-content-classes 接口由调用平台接口改为查询 gjj_ywnrfl 表。
- 查询时按当前 gjsjsf 过滤，仅返回启用数据，按 pxh、id 排序。
- 返回结构保持不变：value 对应 flbm，label 对应 flmc。

#### 2.3 存储方式

- gjj_ywbzk.ywnrfl 和 gjj_ywbz.ywnrfl 继续保存分类编码。
- 本次仅调整分类数据来源，不调整现有字段结构。

#### 2.4 分类维护能力

需新增 gjj_ywnrfl 的基础 CRUD 接口，支持新增、修改、删除。

维护约束：

| 约束项 | 说明 |
| --- | --- |
| 关键算法必填 | 新增和修改时必须选择所属关键算法 gjsjsf |
| 编码唯一性 | flbm 建议在同一关键算法下唯一 |
| 删除校验 | 删除前需校验是否已被标准库或业务规则引用 |

#### 2.5 基础数据初始化

上线前需初始化现有业务内容分类数据，可通过初始化 SQL、后台脚本或数据库手工维护完成。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库页面（server/templates/pages/business_standard.j2） | 业务内容分类下拉改为调用自有接口；下拉按当前关键算法联动过滤；新增工具栏按钮“业务内容分类维护”；维护弹窗支持新增、修改、删除；新增、修改表单中增加“关键数据算法”下拉。 |
| 业务规则页面（server/templates/pages/business_rule.j2） | 查询区业务内容分类下拉改为调用自有接口；下拉按当前关键算法联动过滤；清册选择弹窗中的业务内容分类筛选改为调用自有接口；列表中的业务内容分类名称改为基于自有数据源展示。 |

页面边界：本次不新增独立的业务内容分类管理页面，统一通过标准库页面按钮弹窗维护。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | 新增 gjj_ywnrfl 表及 gjsjsf 字段 |
| 页面模板 | server/templates/pages/business_standard.j2；server/templates/pages/business_rule.j2 |
| 后端逻辑 | server/routes/http/tools.js 及业务内容分类 CRUD、查询、联动过滤逻辑 |
| 兼容性说明 | 不调整 gjj_ywbzk.ywnrfl、gjj_ywbz.ywnrfl 字段结构；不改变现有分类编码存储方式；历史数据可继续使用；上线前需完成 gjj_ywnrfl 基础数据初始化。 |

### 5. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 2”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 执行前仍建议先备份目标库。
- `gjj_ywnrfl` 基础数据初始化可继续参考 `server/data/seed_ywnrfl_gjsjsf_1_oracle.sql` 并按现场库方言转换执行。

## 改造点 3：标准库新增自定义编码字段

### 1. 改造目标

在标准库中新增稳定、可维护的业务标准编码字段，为后续程序和存储过程按编码识别业务办理标准提供统一基础。

### 2. 逻辑改造

#### 2.1 新增字段

在 gjj_ywbzk 表中新增字段：zdybm

字段要求：

| 要求项 | 说明 |
| --- | --- |
| 可维护 | 支持手工录入和修改 |
| 唯一性 | 同一标准库内保持唯一 |
| 取值规范 | 建议使用英文、数字、下划线等稳定字符 |

#### 2.2 标准库读写逻辑

标准库相关接口和导入导出逻辑需同步支持 zdybm：

- 列表查询
- 详情查询
- 新增保存
- 编辑保存
- 导入导出

#### 2.3 后续使用方式

后续程序或存储过程识别业务标准时，应优先基于 zdybm 判断，不再优先依赖 ywblbz、id 或页面展示名称。

#### 2.4 历史数据处理

字段新增后，需为已有标准库数据逐步补齐编码；若后续逻辑依赖 zdybm，则对应标准必须先完成补码。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库页面（server/templates/pages/business_standard.j2） | 查询区增加自定义编码筛选；新增、编辑弹窗增加“自定义编码”录入项；列表增加“自定义编码”展示列。 |
| 业务规则页面 | 本次原则上不新增维护入口；如有需要，可在后续版本增加只读展示。 |

页面边界：本次只解决标准库自定义编码可维护的问题，不扩展为独立编码管理体系。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | gjj_ywbzk 表新增 zdybm 字段 |
| 页面模板 | server/templates/pages/business_standard.j2 |
| 后端逻辑 | server/routes/http/ywbzk.js、导入导出逻辑、后续按编码识别标准的程序逻辑 |
| 兼容性说明 | 不改变现有 gjj_ywbzk.id 的主键含义；历史数据未补充 zdybm 时仍可查询和展示；依赖 zdybm 的后续逻辑上线前需先补齐相关标准编码。 |

### 5. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 3”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 建议先补齐或清理历史重复编码，再执行唯一索引创建。
- 字段新增后可分批补齐历史 `zdybm`。

## 改造点 4：标准库新增业务办理分类字段并优化新增、修改页面展示

### 1. 改造目标

在标准库中新增“业务办理分类”字段，用于区分标准类模板和条件类模板，并根据不同分类动态调整新增、修改页面的表单展示。

### 2. 逻辑改造

#### 2.1 新增字段

在 gjj_ywbzk 表中新增字段：ywblfl

建议取值：

| 编码 | 名称 |
| --- | --- |
| 1 | 标准 |
| 2 | 条件 |

#### 2.2 标准库读写逻辑

标准库相关接口和导入导出逻辑需同步支持 ywblfl。

#### 2.3 条件类模板执行语句

当 ywblfl=1 时，页面和数据保存方式保持现状。  
当 ywblfl=2 时，页面下方内容切换为“业务条件执行语句”，仍保存到 gjj_ywbzk.ywbzjg。

#### 2.4 展示约束

- 选择“标准”时保留当前录入方式。
- 选择“条件”时切换为条件类录入方式。
- 条件类模板保存时，执行语句仍写入 ywbzjg。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库新增页面（server/templates/pages/business_standard.j2） | 在“业务内容分类”下方新增“业务办理分类”下拉；选择“标准”时保留当前表单；选择“条件”时切换为“业务条件执行语句”录入模式。 |
| 标准库修改页面（server/templates/pages/business_standard.j2） | 编辑弹窗同步增加“业务办理分类”下拉；正确回显历史分类值；根据分类值动态调整页面下方展示内容。 |

页面展示建议：

- 业务办理分类放置在“业务内容分类”和“业务办理标准分类”之间。
- 标准类保留现有标准值、结果执行语句等内容。
- 条件类展示“业务条件执行语句”，并减少无关干扰项。

页面边界：本次仅调整标准库新增、修改页面，不扩展到业务规则页面。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | gjj_ywbzk 表新增 ywblfl |
| 页面模板 | server/templates/pages/business_standard.j2 |
| 后端逻辑 | server/routes/http/ywbzk.js 及标准库导入导出逻辑 |
| 兼容性说明 | 不新增新的执行语句字段，继续复用 ywbzjg；历史数据如未设置 ywblfl，需补默认值；选择“标准”时现有录入和保存方式保持不变。 |

### 5. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 4”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 历史空值建议统一补为默认值 `1`。
- `1` 表示“标准”，`2` 表示“条件”。

## 改造点 5：业务办理标准支持配置互斥关系

### 1. 改造目标

支持在标准库中配置业务办理标准之间的互斥关系，并在业务规则页面勾选标准时限制同一业务内容分类下的互斥标准不能同时选中。

### 2. 逻辑改造

#### 2.1 新增互斥关系表

建议新增表：gjj_ywbzkhc

建议字段：

| 字段名 | 说明 |
| --- | --- |
| id | 主键 |
| mbid | 当前标准库模板 ID |
| hcmbid | 互斥标准库模板 ID |
| cjsj | 创建时间 |
| gxsj | 更新时间 |

#### 2.2 配置范围约束

互斥关系建议限制在：

- 同一关键数据算法
- 同一业务内容分类
- 非自身模板

#### 2.3 标准库读写逻辑

标准库详情、保存、删除、导入导出需同步支持互斥关系数据。

#### 2.4 勾选校验

业务规则页面勾选标准时，若与已选标准存在互斥关系，则不允许同时选中，并给出明确提示。

#### 2.5 后端兜底校验

业务规则批量保存接口需增加互斥关系兜底校验，防止绕过前端直接写入冲突数据。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库页面（server/templates/pages/business_standard.j2） | 新增、编辑页面增加“互斥业务办理标准”配置项；互斥可选项过滤为同一关键算法、同一业务内容分类下的其他标准。 |
| 业务规则页面（server/templates/pages/business_rule.j2） | 清册选择弹窗勾选时增加互斥限制；已选择某标准后，其互斥标准禁止再次勾选或自动取消勾选；页面给出明确提示。 |

页面边界：本次只支持互斥关系，不扩展为依赖、包含、优先级覆盖等更复杂的规则组合。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | 新增 gjj_ywbzkhc 表 |
| 页面模板 | server/templates/pages/business_standard.j2；server/templates/pages/business_rule.j2 |
| 后端逻辑 | server/routes/http/ywbzk.js、server/routes/http/ywbz.js 及互斥校验逻辑 |
| 兼容性说明 | 未配置互斥关系时仍按原逻辑使用；新增互斥关系后，仅限制冲突标准不能同时勾选；历史冲突数据需在上线前清理或在页面加载时提示。 |

### 5. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 5”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 请在 `gjj_ywbzk` 主表结构稳定后再执行该段。
- 如现场已有历史冲突数据，请先清理，再启用互斥限制。

## 改造点 6：关键数据算法按不同子存储过程执行

### 1. 改造目标

将当前集中在同一个主存储过程中的关键算法执行逻辑，调整为“统一入口分发 + 不同关键算法调用不同子存储过程”的方式，降低耦合并提升扩展性。

### 2. 逻辑改造

#### 2.1 保留统一入口

统一入口过程继续作为外部调用入口，负责接收入参、按关键算法编码分发到对应子存储过程，并汇总最终结果。

#### 2.2 拆分子存储过程

建议按算法拆分为独立子存储过程：

- 最大可提取额
- 最高可贷金额
- 最高可贷年限
- 借款人最大可对冲支取金额

后续新增算法时，继续按相同模式增加子存储过程。

#### 2.3 通用能力下沉

以下能力建议继续保留为公共函数或公共逻辑：

- 方言 SQL 解析
- 标准值解析
- 页面参数读取
- 动态 SQL 预处理
- 执行日志写入

#### 2.4 调用关系

- 外部程序继续调用统一入口。
- 统一入口按算法编码分发。
- 子存储过程只处理本算法相关逻辑。

#### 2.5 历史逻辑迁移

建议按算法逐步迁移：

- 先抽离一个算法作为样板
- 验证入口分发和日志输出
- 再逐步迁移其他算法

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 业务规则页面（server/templates/pages/business_rule.j2） | 调试功能继续保留统一入口调用方式；调试结果和日志应能体现实际调用的子存储过程或算法分支。 |
| 算法说明区域 | 页面中的关键算法说明应与拆分后的子存储过程执行方式保持一致。 |

页面边界：本次页面仅做调试和说明层面的配套调整，不改变标准库页面和业务规则页面主流程。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 存储过程与函数 | 统一入口、各算法子存储过程、公共函数 |
| 页面模板 | server/templates/pages/business_rule.j2 |
| 后端逻辑 | 调试接口、调试日志展示、直接调用存储过程的后端服务 |
| 兼容性说明 | 建议保留统一入口过程，降低外部接口变更范围；历史调用方可继续通过统一入口执行；拆分后各算法结果口径应与现有逻辑保持一致。 |

## 改造点 7：第三方程序对接范围补齐

### 1. 改造目标

补齐第三方程序侧对关键算法的接入范围。

当前现状：

| 场景 | 现状 |
| --- | --- |
| 业务运行配置 | 已通过 API 接口实现对关键算法的引用 |
| 事项定义工具 | 尚未对接 |
| 定时任务 | 尚未对接 |

### 2. 逻辑改造

#### 2.1 业务运行配置

保持现有调用方式不变，并作为事项定义工具和定时任务对接的参考模式。

#### 2.2 事项定义工具

需补齐以下能力：

- 引用关键算法
- 传入事项场景参数
- 获取关键算法执行结果

#### 2.3 定时任务

需补齐以下能力：

- 引用关键算法
- 按预设参数或业务上下文触发执行
- 记录执行结果和异常信息

#### 2.4 统一调用方式

建议第三方程序统一通过同一套关键算法调用入口接入，统一调用入口、入参格式、返回结构以及异常和日志处理方式。

#### 2.5 对接验证

建议至少验证以下内容：

- 业务运行配置调用结果是否保持一致
- 事项定义工具是否能正确引用并执行关键算法
- 定时任务是否能正确触发关键算法执行
- 异常场景下是否能记录日志并返回清晰错误信息

### 3. 页面改造

| 页面或工具 | 调整内容 |
| --- | --- |
| 业务运行配置页面 | 原则上保持现有页面调用方式不变；如调用入口调整，同步调整调用说明或参数配置方式。 |
| 事项定义工具页面 | 补齐关键算法引用能力，支持选择或配置关键算法，并传递事项上下文参数。 |
| 定时任务配置页面 | 补齐关键算法引用能力，支持配置关键算法调用并查看执行结果或异常状态。 |

页面边界：本次重点是补齐外围系统调用入口，不改变标准库和业务规则页面主流程，也不新增新的关键算法配置模型。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 第三方程序 | 业务运行配置、事项定义工具、定时任务 |
| 后端逻辑 | 统一调用入口、调用适配、日志和异常处理 |
| 页面或工具界面 | 事项定义工具页面、定时任务页面，以及业务运行配置页面的说明调整 |
| 兼容性说明 | 已对接的业务运行配置应继续保持可用；新增事项定义工具和定时任务对接后，不应影响现有关键算法主流程；建议通过统一调用入口兼容不同第三方程序。 |

## 改造点 8：业务办理标准值在清册选择列表中按 X 占位反显

### 1. 改造目标

在业务规则页面“清册选择”弹窗中，业务办理标准列不再直接展示标准库模板原文案，而是按模板中的 `X` 占位符反显当前业务标准实际值，方便业务人员在勾选前直接看到最终业务标准描述。

研发约束：

- 使用业务办理标准值的模板文案统一使用字面量 `X` 作为占位符。
- 本次实现仅负责将 `X` 替换为最终业务标准值，不扩展其他占位语法。

### 2. 逻辑改造

#### 2.1 数据来源

- 标准库 `gjj_ywbzk.ywbzz` 字段保存的是公共参数 ID，不是最终展示值。
- 典型值形如：`1_bz_1762242182359`。
- 需调用 `server/services/gatewayService.js` 中的 `fetchPublicParamValue(publicParamId, organizationNumber, zjgbh, headers)` 查询最终业务标准值。
- 展示时统一取返回结果中的 `value` 字段作为最终替换值。

#### 2.2 替换规则

- 当模板文案 `ywblbz` 中包含 `X`，且 `ywbzz` 能查询到有效 `value` 时，将文案中的全部 `X` 替换为该值。
- 当模板文案不包含 `X` 时，保持原始文案不变，不调用公共参数查询。
- 当 `ywbzz` 为空、公共参数未查到值、或接口调用异常时，保留原始模板文案，不替换为空串。
- 本次仅处理清册选择弹窗展示，不改变数据库中保存的 `ywblbz` 原始模板文案。

#### 2.3 后端处理

`/ywbz/selection_list` 接口需补齐展示字段组装逻辑：

- 查询标准库清册列表时，保留原始字段 `ywblbz`。
- 增加返回字段 `display_ywblbz`，作为前端最终展示文案。
- 对当前返回结果中“包含 `X` 且 `ywbzz` 非空”的记录，按 `ywbzz` 去重后批量调用 `fetchPublicParamValue`。
- 将查询到的 `value` 替换到对应模板文案中，生成 `display_ywblbz`。
- 查询失败时仅记录 warning 日志，接口整体仍正常返回列表数据。

#### 2.4 查询与兼容说明

- 清册选择弹窗原有筛选条件仍按原始字段 `ywblbz`、`ywblbzsm`、`gjsjsf`、`ywnrfl` 查询，不改为按替换后的展示文案检索。
- 互斥关系、已选中状态、排序、分页等现有逻辑保持不变。
- 外层主清册列表、节点详细参数设置抽屉、标准库页面均不在本次范围内。

#### 2.5 数据影响说明

- 本次不新增数据库表，不新增字段。
- 继续复用 `gjj_ywbzk.ywbzz` 存储公共参数 ID。
- 不改变 `gjj_ywbzk.ywblbz` 的原始模板文案存储方式。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 业务规则页面（server/templates/pages/business_rule.j2） | “清册选择”弹窗中的“业务办理标准”列改为展示 `display_ywblbz`，反显替换 `X` 后的最终业务标准文案。 |
| 后端接口（server/routes/http/ywbz.js 的 `/selection_list`） | 返回清册数据时保留原始 `ywblbz`，并新增 `display_ywblbz` 供前端展示。 |

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 页面模板 | server/templates/pages/business_rule.j2 |
| 后端逻辑 | server/routes/http/ywbz.js 的 `/selection_list` 展示值组装逻辑；server/services/gatewayService.js 的公共参数值查询能力复用 |
| 数据范围 | 标准库模板文案 `ywblbz`、公共参数 ID `ywbzz`、接口展示字段 `display_ywblbz` |
| 兼容性说明 | 查不到公共参数值时继续展示原始模板文案；不影响外层主清册列表；不影响节点详细参数设置抽屉；不改变现有数据库结构。 |

### 5. 实施建议

| 阶段 | 建议内容 |
| --- | --- |
| 第一步 | 梳理标准库模板中使用业务标准值的文案，统一替换为 `X` 占位写法。 |
| 第二步 | 后端在 `/selection_list` 中补齐“公共参数 ID -> 实际值 -> 展示文案”转换逻辑，并增加查询失败回退处理。 |
| 第三步 | 前端将清册选择弹窗中的“业务办理标准”列切换为 `display_ywblbz`。 |
| 第四步 | 准备 1 至 2 组包含公共参数 ID 的历史模板数据，验证正常替换、空值回退、异常回退三类场景。 |

### 6. 已上线数据库投产 SQL

本改造点仅调整清册展示逻辑，复用现有 `gjj_ywbzk.ywbzz` 字段和公共参数查询能力，不涉及新增数据库 DDL。

- Oracle：无需执行数据库 DDL。
- 达梦：无需执行数据库 DDL。
- 人大金仓：无需执行数据库 DDL。

## 改造点 9：调试功能支持业务参数自动生成及成功案例保存反显

### 1. 改造目标

在关键数据计算模型的调试功能中，调试 JSON 入参不再完全依赖人工手写，而是根据当前关键数据算法、业务内容分类及业务参数定义自动生成基础入参模板。配置人员只需按调试需要修改参数值即可发起验证。同时支持将调试成功的请求参数保存为案例，供其他人员直接选择、反显并复用。

### 2. 逻辑改造

#### 2.1 调试入参自动生成

- 打开调试弹窗时，系统根据当前算法、业务内容分类和调试参数定义自动生成 JSON 模板。
- 模板中默认带出必要业务参数，例如关键算法编码、业务内容分类、机构编号及该算法对应的常用业务字段。
- 对于可推导的字段优先自动填充默认值或示例值，减少人工拼装成本。

#### 2.2 成功案例保存

- 当一次调试执行成功后，支持将本次请求参数保存为“调试成功案例”。
- 保存内容建议至少包括：算法编码、业务内容分类、案例名称、请求参数 JSON、调试结果摘要、创建人、创建时间。
- 同一算法下可保存多条案例，便于覆盖不同业务场景。

#### 2.3 案例反显与复用

- 调试弹窗中增加“选择案例”或“历史成功案例”入口。
- 选中某个成功案例后，自动将其请求参数反显到 JSON 编辑器中，配置人员可直接再次调试，或在此基础上修改少量字段后验证。
- 反显时保留案例原始结构，避免字段缺失或顺序混乱影响理解。

#### 2.4 前端改造点

- `server/templates/pages/business_rule.j2` 中调试弹窗需增加“自动生成入参”“保存案例”“选择案例/反显案例”相关交互。
- JSON 编辑器初始化时优先加载自动生成模板；选择案例后覆盖当前编辑区内容。
- 调试成功后按钮区支持一键保存当前请求参数为案例。
- 案例列表建议展示案例名称、算法、业务内容分类、创建人、创建时间，并支持点击后反显。

#### 2.5 后端改造点

- 新增调试入参模板生成接口，按算法和业务内容分类返回推荐 JSON 结构。
- 新增调试案例保存、查询、详情接口，支持按算法、业务内容分类过滤。
- 保存案例时建议校验 JSON 合法性，并限制仅保存调试成功的数据，避免无效样例沉淀。
- 详情接口返回时需保持 JSON 原始结构，确保前端反显一致。

#### 2.6 数据影响说明

- 建议新增调试案例表，按算法、业务内容分类和案例维度存储请求参数样例。
- 如暂不建表，也可先采用 JSON 文件或其他配置存储，但长期建议落库以支持检索、权限和审计。
- 历史未保存案例的数据不受影响，原有手工调试方式仍可保留作为兜底。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 业务规则页面（server/templates/pages/business_rule.j2） | 调试弹窗打开时自动生成 JSON 入参模板；增加保存成功案例、选择案例、案例反显能力。 |
| 调试案例选择区域 | 增加案例下拉或列表，支持按算法和业务内容分类筛选，并将选中案例回填到 JSON 编辑器。 |

示意截图：

![关键数据计算模型调试参数自动生成与案例反显示意](assets/business-rule-debug-param-example.png){ width=90% }

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 页面模板 | server/templates/pages/business_rule.j2 |
| 后端逻辑 | 调试模板生成接口、调试执行接口、案例保存查询接口 |
| 数据范围 | 调试请求参数 JSON、案例基础信息、调试结果摘要 |
| 兼容性说明 | 保留原有手工填写 JSON 的能力；未配置自动生成模板时可回退为空模板；未保存案例时不影响现有调试流程。 |

### 5. 实施建议

| 阶段 | 建议内容 |
| --- | --- |
| 第一步 | 梳理当前各关键算法调试所需最小参数集，形成自动生成模板规则。 |
| 第二步 | 完成案例表或案例存储结构设计，并补齐保存、查询、详情接口。 |
| 第三步 | 前端增加模板初始化、案例选择、案例反显及保存按钮交互。 |
| 第四步 | 选取 2 至 3 个典型算法场景沉淀首批成功案例，验证复用效果。 |

### 6. 接口示例

#### 6.1 调试模板生成接口

请求示例：

```json
{
  "ywsf": "1",
  "ywnrfl": "1",
  "jgbh": "3607000000",
  "zjgbh": "3607000000"
}
```

返回示例：

```json
{
  "status": 0,
  "msg": "ok",
  "data": {
    "template": {
      "ywsf": "1",
      "ywnrfl": "1",
      "jgbh": "3607000000",
      "zjgbh": "3607000000",
      "dkje": "",
      "fwzj": ""
    }
  }
}
```

#### 6.2 调试案例保存接口

请求示例：

```json
{
  "ywsf": "1",
  "ywnrfl": "1",
  "case_name": "购房提取-首套房成功案例",
  "request_json": "{\"ywsf\":\"1\",\"ywnrfl\":\"1\",\"jgbh\":\"3607000000\"}",
  "result_summary": "调试成功，结果=500000"
}
```

返回示例：

```json
{
  "status": 0,
  "msg": "保存成功",
  "data": {
    "id": "10001"
  }
}
```

#### 6.3 调试案例详情接口

返回示例：

```json
{
  "status": 0,
  "msg": "ok",
  "data": {
    "id": "10001",
    "case_name": "购房提取-首套房成功案例",
    "ywsf": "1",
    "ywnrfl": "1",
    "request_json": {
      "ywsf": "1",
      "ywnrfl": "1",
      "jgbh": "3607000000",
      "zjgbh": "3607000000"
    }
  }
}
```

### 7. 测试点清单

| 测试项 | 检查内容 |
| --- | --- |
| 模板生成 | 进入调试弹窗后，是否按当前算法和业务内容分类自动生成 JSON 模板 |
| 模板准确性 | 自动生成字段是否覆盖当前算法所需最小参数集 |
| 参数编辑 | 自动生成后的各字段是否允许手工修改并正常提交 |
| 调试执行 | 使用模板参数和修改后参数均可成功发起调试 |
| 成功案例保存 | 仅调试成功的数据允许保存，失败结果不可保存为案例 |
| 案例列表查询 | 按算法、业务内容分类、案例名称过滤是否生效 |
| 案例反显 | 选中案例后，JSON 编辑器是否完整回填原始参数结构 |
| 二次调试 | 反显后的案例是否可直接再次调试，并支持修改后重试 |
| 数据持久化 | 保存后的案例重新打开页面后仍可查询和反显 |
| 兼容性 | 不使用案例功能时，原有手工 JSON 调试流程是否保持可用 |

### 8. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 9”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 该段会创建调试成功案例表 `gjj_ywbz_debug_case` 及范围索引。

## 改造点 10：标准库新增提示语属性名称和提示语属性单位

### 1. 改造目标

在标准库模板中新增“提示语属性名称”和“提示语属性单位”两个字段，用于补充业务办理标准的提示语展示信息，便于后续页面提示、结果展示或外围系统按统一字段读取。

本次改造以标准库模板定义为核心，优先完成数据库字段、标准库新增/编辑页面以及保存接口的支持，历史数据保持兼容。

### 2. 逻辑改造

#### 2.1 数据库字段调整

在 `gjj_ywbzk` 表中新增以下两个字段：

| 字段名 | 类型建议 | 说明 |
| --- | --- | --- |
| tsysxmc | VARCHAR(200) | 提示语属性名称 |
| tsysxdw | VARCHAR(100) | 提示语属性单位 |

两个字段均为非必填字段，未配置时允许为空。

#### 2.2 标准库保存逻辑

标准库保存接口 `/ywbzk/save` 需同步接收并处理 `tsysxmc`、`tsysxdw` 两个参数：

- 新增时写入 `gjj_ywbzk`。
- 编辑时同步更新 `gjj_ywbzk`。
- 入库前建议统一进行 `trim` 处理，空字符串按空值保存。

本次改造不改变现有模板主键、属性组、互斥关系及业务分类等已有逻辑。

#### 2.3 标准库详情回显

标准库详情接口 `/ywbzk/get` 继续通过 `SELECT *` 返回模板主表信息，因此新增字段可随模板详情一并返回，供编辑弹窗自动回显。

历史数据由于这两个字段为空，不会影响已有页面打开、编辑和保存流程。

#### 2.4 业务含义约束

- `tsysxmc` 用于描述提示语对应的属性名称，例如“最高可贷额度”“可提取金额”等。
- `tsysxdw` 用于描述提示语展示单位，例如“元”“万元”“年”等。
- 本阶段仅补齐模板定义与录入能力，不强制要求业务规则执行链路立即消费该字段。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库新增页面（server/templates/pages/business_standard.j2） | 在“业务办理标准”后新增“提示语属性名称”“提示语属性单位”两个输入框，支持与其他模板基础字段一起提交。 |
| 标准库编辑页面（server/templates/pages/business_standard.j2） | 编辑弹窗同步增加上述两个输入框，并支持历史数据回显、修改和保存。 |

页面展示建议：

- 两个字段放置在“业务办理标准”和“自定义编码”之间，便于与模板基础定义集中维护。
- 两个字段均可为空，不增加必填限制。
- 输入框建议保持普通文本框形态，避免过早绑定固定字典。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | `gjj_ywbzk` 新增 `tsysxmc`、`tsysxdw` 两个字段 |
| 页面模板 | server/templates/pages/business_standard.j2 |
| 后端逻辑 | server/routes/http/ywbzk.js 的新增、编辑保存逻辑 |
| 测试范围 | 标准库模板页面结构测试；标准库保存接口新增/编辑测试 |
| 兼容性说明 | 新字段均允许为空；历史数据无需回填默认值；未配置提示语字段时不影响现有模板查询、编辑、规则配置和执行流程。 |

### 5. 测试点清单

| 测试项 | 检查内容 |
| --- | --- |
| 新增录入 | 标准库新增弹窗中是否展示“提示语属性名称”“提示语属性单位”输入框，并可正常提交 |
| 编辑回显 | 编辑已有模板时，已保存的提示语字段是否能正确回显 |
| 编辑更新 | 修改提示语字段后再次保存，数据库中对应值是否被正确更新 |
| 空值兼容 | 两个字段留空时，新增和编辑是否仍可正常保存 |
| 历史数据兼容 | 历史未配置提示语字段的模板是否仍可正常打开和保存 |
| 接口兼容 | `/ywbzk/save`、`/ywbzk/get` 在新增字段后是否保持原有返回结构兼容 |

### 6. 已上线数据库投产 SQL

本改造点的投产 SQL 已拆分到独立脚本，请按数据库类型执行对应文件中的“改造点 10”段落：

| 数据库 | 脚本 |
| --- | --- |
| Oracle | `server/data/upgrade_business_standards_online_oracle.sql` |
| 达梦 | `server/data/upgrade_business_standards_online_dm.sql` |
| PostgreSQL | `server/data/upgrade_business_standards_online_pg.sql` |
| GaussDB | `server/data/upgrade_business_standards_online_gauss.sql` |
| 人大金仓 | `server/data/upgrade_business_standards_online_kingbase.sql` |

补充说明：

- 该段主要为 `gjj_ywbzk` 增加 `tsysxmc`、`tsysxdw` 两个字段。
- 两个字段均允许为空，投产时无需补默认值。

## 改造点 11：标准库属性组支持自定义属性录入

### 1. 改造目标

在标准库页面的“业务办理标准属性组”中，除现有“对象属性”选择方式外，新增“自定义属性”录入方式。

当选择“自定义属性”时，允许直接录入“属性名称”和“属性编码”，并继续复用现有 `gjj_ywbzksx` 表保存配置；同时业务规则页面在配置参数时，应支持对这类自定义属性手工录入值。

### 2. 逻辑改造

#### 2.1 页面模式切换

标准库新增、修改页面中的“业务办理标准属性组”区域调整为 3 列展示，并新增“是否对象属性”开关，默认值为“是”。

- 选择“是”时，保持现有逻辑不变，继续选择所属对象、业务标准属性、属性来源及来源 SQL。
- 选择“否”时，隐藏“所属对象”“业务标准属性”“属性来源”“来源 SQL”等现有对象属性相关项，仅保留“属性名称”“属性编码”两个输入项。

#### 2.2 配置存储方式

本次改造继续使用现有表：`gjj_ywbzksx`，不新增表，不新增“是否对象属性”数据库字段。

当“是否对象属性=是”时，按当前方式保存，不调整现有字段含义。

当“是否对象属性=否”时，建议按下表保存：

| gjj_ywbzksx 字段 | 保存值 | 说明 |
| --- | --- | --- |
| ywblbzdx | 空 | 不关联对象属性 |
| fwdxbq | 空 | 不关联对象标签 |
| sxbm | 属性名称 | 作为页面展示名称 |
| ywblbzsx | 属性编码 | 作为参数编码和取值键 |
| sxly | page | 继续按页面取值处理 |
| ywblbzyg | 空 | 不再配置来源 SQL |

说明：

- 本次不建议新增 `sxly=zdy` 之类的新取值。
- `sxly` 仍保持现有语义，仅使用 `page/sql` 两种模式。
- 自定义属性固定按 `page` 处理，可直接复用现有参数保存和存储过程取值链路。

#### 2.3 编辑回显规则

标准库编辑页面无需额外新增数据库标识字段，可直接通过 `gjj_ywbzksx.ywblbzdx` 是否为空判断当前记录类型：

- `ywblbzdx` 有值：按“对象属性”模式回显。
- `ywblbzdx` 为空：按“自定义属性”模式回显，并回填“属性名称”“属性编码”。

#### 2.4 业务规则页参数配置

业务规则页面中的参数配置区域继续根据 `gjj_ywbzksx` 动态生成录入控件，但需区分两类属性：

- 当 `ywblbzdx` 有值时，保持当前对象属性方式，继续渲染为下拉选择。
- 当 `ywblbzdx` 为空时，按自定义属性方式渲染为文本输入框，允许直接录入对应值。

参数保存时仍以 `ywblbzsx` 作为参数编码，以 `sxbm` 作为展示名称，不额外引入新的参数结构。

#### 2.5 存储过程兼容性

由于自定义属性统一保存为 `sxly=page`，现有存储过程和函数中基于 `page` 分支读取页面参数的逻辑可继续复用。

因此本次改造无需为“自定义属性”单独扩展新的存储过程分支，也无需引入额外数据库执行语义。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 标准库页面（server/templates/pages/business_standard.j2） | “业务办理标准属性组”区域调整为 3 列布局，新增“是否对象属性”开关；选择“是”时保留当前对象属性配置方式；选择“否”时切换为“属性名称”“属性编码”录入方式，并隐藏“属性来源”“来源 SQL”等无关项。 |
| 业务规则页面（server/templates/pages/business_rule.j2） | 参数配置区域需支持两种渲染方式：对象属性继续使用下拉，自定义属性改为文本输入框，并支持录入后正常保存、反显。 |

页面展示建议：

- “是否对象属性”建议放置在属性组区域首列，默认值为“是”。
- 选择“否”后，页面仅保留当前场景必填项，减少无关干扰。
- 自定义属性的“属性编码”需增加非空校验和唯一性校验，避免同一模板下重复。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 数据库结构 | 不新增表，不新增字段，继续复用 `gjj_ywbzksx` |
| 页面模板 | server/templates/pages/business_standard.j2；server/templates/pages/business_rule.j2 |
| 后端逻辑 | 标准库属性组新增、编辑、回显逻辑；业务规则参数配置的动态渲染和保存逻辑 |
| 存储过程 | 无需新增“自定义属性”专用分支，继续复用 `sxly=page` 现有逻辑 |
| 兼容性说明 | 历史对象属性数据不受影响；新增自定义属性记录通过 `ywblbzdx` 为空进行识别；`sxly` 保持现有 `page/sql` 语义，不引入新的来源类型。 |

### 5. 已上线数据库投产 SQL

本改造点在采用上述方案时，仅复用现有字段含义，不涉及数据库 DDL 调整。

- Oracle：无需执行数据库 DDL。
- 达梦：无需执行数据库 DDL。
- 人大金仓：无需执行数据库 DDL。

## 改造点 12：业务规则页面新增 AI 政策分析辅助清册勾选

### 1. 改造目标

在业务规则页面“清册选择”弹窗中新增“AI 政策分析”能力，支持业务人员输入政策文本后，系统结合当前关键数据算法、业务内容分类、候选业务办理标准及已选结果发起 AI 分析，并返回建议勾选的业务办理标准列表，辅助完成清册筛选。

本次改造目标是补齐“政策文本 -> AI 分析 -> 勾选结果同步 -> 用户提示”这一完整辅助链路，不替代人工最终确认，也不改变现有业务规则保存模型。

### 2. 逻辑改造

#### 2.1 分析入口与触发条件

- AI 政策分析入口放置在业务规则页面“清册选择”弹窗的查询区域。
- 仅当已选择关键数据算法，且最大可提取额场景已选择业务内容分类时，允许发起分析。
- 用户输入政策文本后，点击“AI 政策分析”按钮发起分析请求。

#### 2.2 AI 输入上下文

AI 分析时，需将以下上下文一并传递给后端，再由后端组装为工作流输入：

| 输入项 | 说明 |
| --- | --- |
| 政策文本 | 用户在弹窗中录入的原始政策内容 |
| 当前关键数据算法 | 对应 `ywsf/gjsjsf` |
| 当前业务内容分类 | 对应 `ywnrfl` |
| 清册筛选条件 | 当前输入的业务办理标准、业务办理标准描述关键字 |
| 候选标准列表 | 当前算法和业务内容分类下的可选业务办理标准 |
| 已选结果 | 当前机构下已同步到业务规则表的标准结果 |

说明：

- 候选标准列表用于帮助 AI 在限定范围内输出更准确的勾选结果。
- AI 输出结果原则上仅返回建议勾选的标准 ID 列表及简短说明。
- AI 不直接写库，仍由后端统一做结果校验和同步。

#### 2.3 Dify 工作流配置页面

AI 政策分析能力需接入现有 Dify 工作流配置页面进行维护，支持新增和修改配置。

配置要求建议如下：

| 配置项 | 说明 |
| --- | --- |
| page_key | 固定配置为业务规则页面对应标识，例如 `business_rule` |
| workflow_name | 工作流名称，例如“政策分析” |
| workflow_type | 需在配置页面中选择 `ai_analysis`，用于标识 AI 分析类型 |
| api_url | Dify 工作流调用地址 |
| api_key | Dify 工作流密钥 |
| enabled | 是否启用 |
| description | 备注说明，可填写“业务规则清册 AI 政策分析” |

配置页面交互要求：

- 在现有“AI 工作流配置”页面中，新增配置时支持选择工作流类型。
- 修改已有配置时，支持回显并修改 `workflow_type`。
- 当当前页面用途为业务规则清册 AI 分析时，`workflow_type` 需选择或默认带出 `ai_analysis`。
- 同一 `page_key` 下允许按不同 `workflow_type` 维护多条配置，便于后续扩展报告生成、总结归纳等其他 AI 能力。

#### 2.4 后端工作流调用

后端新增 AI 政策分析接口，建议继续使用页面级工作流配置方式：

- 根据页面标识 `page_key` 和工作流类型 `workflow_type` 查询启用中的工作流配置。
- 将政策文本和候选标准列表组装为工作流入参后调用 Dify 工作流。
- 工作流执行成功后，解析返回内容中的 `selectedIds` 和 `summary`。
- 若 AI 未返回可识别的标准 ID，则按失败处理并返回明确提示。

说明：

- Dify 配置相关接口可复用现有 `server/routes/http/dify-config.js`。
- 创建配置时 `workflow_type` 默认值可保持为 `ai_analysis`，但页面上仍建议显式可选、可回显、可修改。

#### 2.5 结果同步与勾选处理

- 后端拿到 `selectedIds` 后，需复用现有业务规则同步逻辑，将 AI 建议结果同步到 `gjj_ywbz`。
- 同步前继续执行互斥关系校验，防止 AI 返回互斥标准导致脏数据入库。
- 同步成功后，接口返回最新勾选 ID 列表及成功提示文案。
- 前端收到成功结果后，自动在清册弹窗中勾选当前页命中项，并保留后续人工调整能力。

#### 2.6 提示与交互说明

- AI 分析弹窗中增加说明提示，告知用户建议先将每页显示条数设置为最大后再执行分析，以便一次性查看更多命中结果。
- AI 成功后，页面需弹出成功提示，明确说明“已自动勾选当前页命中结果”，并提示可先调整每页显示条数后重新分析。
- AI 失败时，需直接展示失败原因，例如未配置工作流、未返回可匹配标准、存在互斥标准等。

#### 2.7 范围与边界

- 本次 AI 分析仅作用于业务规则页面“清册选择”弹窗，不扩展到标准库页面。
- 本次以“辅助勾选”为目标，不自动代替用户点击“保存”之外的其他确认动作。
- 本次不要求将 AI 返回的完整推理过程长期落库，仅保留必要的同步结果和提示信息。

### 3. 页面改造

| 页面 | 调整内容 |
| --- | --- |
| 业务规则页面（server/templates/pages/business_rule.j2） | 在“清册选择”弹窗查询区增加“AI 政策分析”按钮；点击后弹出政策文本输入框；分析成功后自动同步当前页勾选结果，并弹出成功提示。 |
| AI 政策分析弹窗 | 增加政策文本多行输入框、操作说明提示、开始分析按钮；提示用户建议先将每页显示条数设置为最大后再分析。 |
| AI 工作流配置页面 | 在现有 Dify 工作流配置页面中，新增、编辑配置时支持选择工作流类型；为业务规则页面新增 AI 配置时选择 `AI 分析`，对应保存 `workflow_type=ai_analysis`。 |

页面展示建议：

- 按钮名称建议使用“AI 政策分析”或支持页面级可配置按钮文案。
- 说明提示使用 `info` 样式，避免与错误提示混淆。
- 成功提示建议使用页面 `toast`，保证在弹窗场景下也能及时看到结果反馈。

### 4. 影响范围及兼容性说明

| 类别 | 内容 |
| --- | --- |
| 页面模板 | `server/templates/pages/business_rule.j2` |
| 后端逻辑 | `server/routes/http/ywbz.js` 中 AI 政策分析接口、清册同步逻辑、提示文案；`server/routes/http/dify-config.js` 中配置新增、查询、修改逻辑 |
| 配置与环境 | 页面级工作流配置表 `sys_dify_config`、Dify 地址与密钥配置、工作流类型 `workflow_type=ai_analysis` |
| 测试范围 | 页面模板结构测试、AI 分析接口测试、提示文案与同步结果测试 |
| 兼容性说明 | 未配置 AI 工作流时，原有清册选择和人工勾选流程保持不变；不影响标准库、调试功能及业务规则主保存流程；AI 仅作为辅助筛选能力，不改变现有数据结构。 |

### 5. 实施建议

| 阶段 | 建议内容 |
| --- | --- |
| 第一步 | 梳理 AI 工作流输入输出协议，明确 `selectedIds`、`summary` 等标准返回结构。 |
| 第二步 | 在 Dify 工作流配置页面中支持新增、编辑 `workflow_type`，并为业务规则页面增加 `ai_analysis` 类型配置。 |
| 第三步 | 补齐 `selection_ai_apply` 接口和页面级工作流读取能力，并复用现有规则同步逻辑。 |
| 第四步 | 在业务规则页面增加 AI 政策分析入口、政策文本输入弹窗和成功/失败提示。 |
| 第五步 | 准备 2 至 3 组典型政策文本，验证命中结果、互斥校验、无匹配结果和未配置工作流等场景。 |

### 6. 接口示例

#### 6.1 AI 政策分析请求示例

```json
{
  "page_key": "business_rule",
  "workflow_type": "ai_analysis",
  "policy_text": "购房提取时，购房提取累计提取额按已提取金额计算，材料提取限额为房屋总价减去购房首付款。",
  "ywsf": "1",
  "ywnrfl": "1",
  "ywblbz": "",
  "ywblbzsm": ""
}
```

#### 6.2 AI 政策分析返回示例

```json
{
  "status": 0,
  "msg": "AI 分析完成，已自动勾选当前页命中结果并同步 2 条业务办理标准。建议先将每页显示条数设置为最大后，再重新进行分析。",
  "data": {
    "selectedIds": [84, 85],
    "summary": "建议保留购房提取累计提取额和购房提取材料限额两项标准。",
    "syncResult": {
      "insertCount": 1,
      "deleteCount": 0,
      "keepCount": 1
    }
  }
}
```

### 7. 测试点清单

| 测试项 | 检查内容 |
| --- | --- |
| 配置页新增 | 在 AI 工作流配置页面新增配置时，是否可选择“AI 分析”类型，并正确保存 `workflow_type=ai_analysis` |
| 配置页修改 | 编辑已有配置时，`workflow_type` 是否能正确回显、修改和保存 |
| 入口展示 | 已满足算法和业务内容分类条件时，是否正确展示并启用“AI 政策分析”按钮 |
| 工作流配置 | 已配置 `sys_dify_config` 时是否能正确命中页面工作流；未配置时是否给出清晰错误提示 |
| 请求入参 | 政策文本、算法、业务内容分类、候选标准列表是否完整传递给 AI 工作流 |
| AI 结果解析 | AI 返回 JSON、字符串 JSON、包装结构等场景下，是否都能正确解析 `selectedIds` |
| 同步结果 | AI 建议标准是否能正确同步到 `gjj_ywbz` |
| 互斥校验 | AI 返回互斥标准时，是否能正确拦截并提示 |
| 成功提示 | AI 分析成功后，页面是否弹出成功提示，并带出“每页显示条数设置为最大”的建议 |
| 当前页勾选 | 当前页命中项是否自动勾选，且不影响后续人工调整 |
| 失败回退 | AI 未返回可匹配标准、工作流异常、接口异常时，页面是否能正确提示且不破坏原有勾选状态 |

### 8. 已上线数据库投产 SQL

本改造点复用现有业务规则表和页面级工作流配置表，不涉及新增数据库 DDL。

- Oracle：无需执行数据库 DDL。
- 达梦：无需执行数据库 DDL。
- 人大金仓：无需执行数据库 DDL。
